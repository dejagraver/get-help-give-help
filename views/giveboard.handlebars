
<!-- navigation bar -->
<header>
    <nav>
        <ul>
            <li>
                <a href="/">Home</a>
            </li>
            <li>
                <a href="/giveboard" id="giveboardnav"><u>Give Help Board</u></a>
            </li>
            <li>
                <a href="/getboard" id="getnav">Get Help Board</a>
            </li>
            <li id="login-logout">
                   {{#if loggedIn}}
                <a href="/dashboard">Create New Post</a>
                <button class="btn btn-light" id="logout">logout</button>
                {{else}}
                <a href="/login" id="navlogin">sign up or log in</a>

                {{/if}}
            </li>
        </ul>
    </nav>
</header>
<div>
    <h1>Give Help Board</h1>
</div>
{{!-- posts --}}

{{!-- this is testing area only and can be deleted, anything above has not been touched ---------------------}}
{{!-- this is input area first, you can move it where you like, design it or make it for each post--}}

  {{!-- check if logged in is needed --}}
  {{#each dbPostData as |post|}}
  <li>
    
  </li>
  {{!-- added --}}
  <div>{{post.dataValues.title}}</div>
  <div>{{post.dataValues.content}}</div>
  <div>{{post.dataValues.createdAt}}</div>
  <br>
<br>
  {{/each}}
</ol>
</section>


  